---
title: 'Index'
date: '2023-01-21'
slug: 'index'
---

import ControlledAccordion from '../components/ControlledAccordion.tsx';

# Devs Manual

## React

<ControlledAccordion id="custom-hook" title="Custom Hooks">
`useToggle.js`
```jsx{numberLines:true}
import { useState, useCallback } from 'react';

const useToggle = (defaultState = false) => {
const [enabled, setEnabled] = useState(defaultState);

    const toggle = useCallback(() => {
      setEnabled(current => !current);
    }, []);

    return { enabled, toggle };

};

export default useToggle;
```
[Read More](/docs/custom-hooks)
</ControlledAccordion>

<ControlledAccordion id="higher-order-component" title="Higher-Order Component">

```jsx{numberLines:true}
function withChangeLogger(WrappedComponent) {
  const WithChangeLogger = ({ onChange = () => {}, ...restProps }) => {
    const handleChange = useCallback((value) => {
      Logger.log(value);
      onChange(value);
    }, [onChange]);
    return <WrappedComponent onChange={handleChange} {...restProps} />;
  };
  WithChangeLogger.displayName = `WithChangeLogger(${getDisplayName(WrappedComponent)})`;

  return WithChangeLogger;
}

function getDisplayName(WrappedComponent) {
  return WrappedComponent.displayName || WrappedComponent.name || 'Component';
}
```

[Read More](/docs/higher-order-component)

</ControlledAccordion>

<ControlledAccordion id="render-props" title="Render Props">

```jsx{numberLines:true}
<DataProvider render={data => (
  <h1>Hello {data.target}</h1>
)}/>
```

[Read More](/docs/render-props)

</ControlledAccordion>

## CSS

<ControlledAccordion id="css-stacking-context" title="Stacking Context">
  Stacking context (and stacking order) is crucial to understand how HTML elements are rendered in a certain order, which could result into some elements overlaping the other and could confuse you a lot. If you have experienced  setting some elements with `z-index: 9999999` but still getting overlapped by other elements with lower or no `z-index` value, I strongly recommend you to read the following articles and documents.

- [What No One Told You About Z-Index — Philip Walton](https://philipwalton.com/articles/what-no-one-told-you-about-z-index/)
- [The stacking context - CSS: Cascading Style Sheets | MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Positioning/Understanding_z_index/The_stacking_context)
- [Stacking without the z-index property - CSS: Cascading Style Sheets | MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Positioning/Understanding_z_index/Stacking_without_z-index)

[Read More](/docs/css-stacking-context)

</ControlledAccordion>

## Clean Code

<ControlledAccordion id="clean-code-or-preference" title="Clean Code or Preference?">
TODO

[Clean Code vs Preference by Traversy Media](https://www.youtube.com/watch?v=ou6x2qcLOLI)

</ControlledAccordion>

<ControlledAccordion id="abc" title="Approaches to eliminate if-else or switch-case blocks">
  TODO
</ControlledAccordion>

<ControlledAccordion id="null-value-object" title="Null Value Object">
  TODO
</ControlledAccordion>

<ControlledAccordion id="namespace" title="Namespace">
  TODO
</ControlledAccordion>
